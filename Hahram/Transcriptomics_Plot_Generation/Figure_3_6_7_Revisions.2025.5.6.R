VlnPlot(downsampled.2dto3d, features = "Odc")
#####
Figure_6_OrderGenes <- c(
  "Cxcl2", 
  "Cxcl3",
  "Cxcl6",
  "Cxcl1",
  "Ptgs2",
  "Mastl",
  "Ska3",
  "Trip13",
  "Rrm2",
  "Gen1",
  "Spc25",
  "Pbk",
  "Mms22l",
  "Fen1",
  "Ckap2l",
  "Ska1",
  "Rpa2",
  "Arhgap11a",
  "Angptl4",
  "Adtrp",
  "Pde4c.1",
  "Ncapg2",
  "Neto2",
  "Exo1",
  "Trmt61a",
  "Gins1",
  "Mcm5",
  "Asf1b",
  "Cish",
  "Selenoi",
  "Ube2c",
  "Prc1",
  "Aurka",
  "Taf4b",
  "Odc1",
  "Ptgr1",
  "Lyar",
  "Atf3",
  "Igf1r",
  "Itga6",
  "Ldlr",
  "Lrp8",
  "Nptn",
  "Depdc1b",
  "Zc3h12a",
  "Cnfn",
  "Melk",
  "F2rl2",
  "Mybl2",
  "Acot7",
  "Nfkbia",
  "Il1a",
  "Tnfaip3",
  "Plaur",
  "Tnfrsf12a",
  "Hras",
  "Il24",
  "Lamb3",
  "Nrg1",
  "Rtn4",
  "Vasp"
)
#####
#####
Figure_6_OrderGenes_Interest <- c(
  "Cxcl2",
  "Cxcl3",
  "Cxcl6",
  "Cxcl1",
  "Ptgs2",
  "Angptl4",
  "Adtrp",
  "Pde4c.1",
  "Taf4b",
  "Odc1",
  "Ptgr1",
  "Lyar",
  "Atf3",
  "Igf1r",
  "Itga6",
  "Ldlr",
  "Lrp8",
  "Nptn",
  "Depdc1b",
  "Zc3h12a",
  "Cnfn",
  "Melk",
  "F2rl2",
  "Mybl2",
  "Acot7",
  "Nfkbia",
  "Il1a",
  "Tnfaip3"
)
#####
Figure_7_OrderGenes <- c(
  "Ebf1",
  "Cxcr4",
  "Bcl6",
  "St6gal1",
  "Bhlhe40",
  "Litaf",
  "Ackr3",
  "Runx2",
  "Serpine1",
  "Acap1",
  "Ldlrad4",
  "Pmepa1",
  "Entpd4",
  "Ppp1r14a",
  "Sema4g",
  "Itpr1",
  "Wfdc1",
  "Camk2n1",
  "Smad7",
  "Dlc1",
  "Wwc2",
  "Camta1",
  "Zbtb20",
  "Zfp36l2",
  "Barx2",
  "Parm1",
  "Car9",
  "Tmprss2",
  "Cmbl",
  "Ankub1",
  "Zc3h6",
  "Rhobtb3",
  "Mmp7",
  "Bmp6",
  "Vim",
  "Plxna2",
  "Pik3cb",
  "Bnip3",
  "Ltbp3",
  "Vill",
  "Scgb1a1",
  "Rtn4rl1",
  "Fosl2",
  "Rere",
  "Tsc22d3",
  "Aff1",
  "Ahr",
  "Cux1",
  "Dbp",
  "Runx1",
  "Vegfa",
  "Prom1",
  "Efnb2",
  "Mfge8",
  "Nfat5",
  "Atrn",
  "F3",
  "Itgav",
  "Sdc4",
  "Tspan1",
  "Vtcn1",
  "Epas1",
  "Fzd2",
  "Lamb2",
  "Elf3",
  "Igfbp3",
  "Itgb6",
  "Krt7",
  "Pik3c2a",
  "Foxo1",
  "Furin",
  "Tgfbi",
  "Ephb2",
  "Dlk2",
  "Sulf1",
  "Stra6"
)

Figure_7_OrderGenes_Interest <- c(
  "Ebf1",
  "Cxcr4",
  "Bcl6",
  "St6gal1",
  "Bhlhe40",
  "Litaf",
  "Ackr3",
  "Wfdc1",
  "Camk2n1",
  "Smad7",
  "Dlc1",
  "Wwc2",
  "Camta1",
  "Zbtb20",
  "Zfp36l2",
  "Mmp7",
  "Bmp6",
  "Vim",
  "Plxna2",
  "Pik3cb",
  "Bnip3",
  "Ltbp3",
  "Runx1",
  "Vegfa",
  "Prom1",
  "Efnb2",
  "Mfge8",
  "Vtcn1",
  "Epas1",
  "Fzd2",
  "Lamb2",
  "Elf3",
  "Igfbp3",
  "Itgb6",
  "Krt7",
  "Pik3c2a",
  "Foxo1",
  "Furin",
  "Tgfbi",
  "Ephb2",
  "Dlk2",
  "Sulf1"
)
#######

######
Figure_3_OrderGenes <- c(
"Col18a1",
  "Birc5",
  "Nuf2",
  "Bmp7",
  "Mki67",
  "Epha7",
  "Tgfb2",
  "Tead2",
  "Incenp",
  "Wnt7b",
  "Cd3eap",
  "Zfp57",
  "Cdh3",
  "Lamc1",
  "Nt5dc2",
  "Tk1",
  "Cdk1",
  "Impdh1",
  "Gins2",
  "Ydjc",
  "Mcph1",
  "Lrfn4",
  "Orc6",
  "Efna4",
  "Ncaph",
  "Cks1b",
  "Ifrd2",
  "Lamc2",
  "Stk26",
  "Topbp1",
  "Ankrd52",
  "Nipsnap1",
  "Gcsh",
  "Fjx1",
  "Psat1",
  "Hmmr",
  "Cdkn3",
  "Tpx2",
  "Pttg1",
  "Gpx8",
  "Igf2r",
  "Efnb2",
  "Fn1",
  "Smurf1",
  "Pak1ip1", 
  "Edn3",
  "Thbs1",
  "Pdap1",
  "Qdpr",
  "Gpi",
  "Lamc1",
  "F2r",
  "F2rl1",
  "Dcbld2",
  "Tfrc",
  "Tnfrsf21",
  "E2f3",
  "E2f6",
  "Myef2",
  "Necab3",
  "Tcf3",
  "Zfp444",
  "Nme2",
  "Smarce1",
  "Sub1",
  "Ybx1",
  "Ybx3"
 
)
Figure_3_OrderGenes_Interest<- c(
"Birc5",
"Mki67",
"Epha7",
"Tgfb2",
"Tead2",
"Wnt7b",
"Impdh1",
"Ydjc",
"Mcph1",
"Lrfn4",
"Efna4",
"Lamc2",
"Topbp1",
"Fjx1",
"Psat1",
"Pttg1",
"Igf2r",
"Efnb2",
"Fn1",
"Smurf1",
"Lamc1",
"E2f3",
"E2f6"
)
Embryo.2D.3D.Upreg.edited
Twenty4toFiveD.Upreg.edited
Twenty4toFiveD.Downreg.edit
TwoDto3D.Downreg.edited
TwoDto3D.Upreg.edit

downsampled.embryo.2d <- subset(downsampled, subset = Condition == c("Embryogenesis", "St. Dclk1", "P0-2D", "P3-2D"))
downsampled.embryo.2d <- RenameIdents(downsampled.embryo.2d,
                                      "Embryogenesis" = "Embryo")
downsampled.embryo.2d$Condition <- Idents(downsampled.embryo.2d)
downsampled$Condition
embryo.2d.interest <- c("Tgfb2", "Igf2r", "Epha7", "Impdh1", "Ydjc", "Mcph1", "Lrfn4", "Lamc2", "Topbp1", "Fjx1", "Psat1", "Efnb2", "Fn1", "Smurf1", "Mki67", "Tead2", "Birc5", "Wnt7b")
embryo.2d.interest %in% embryo.2d
embryo.2d <- c(embryo.2d, "Igf2r", "Efnb2", "Fn1", "Smurf1")
remove.embryo.2d <- c("Egfl7")
embryo.2d <- setdiff(embryo.2d, remove.embryo.2d)

VlnPlot(downsampled.embryo.2d, features = "Tnfrsf21")
png(file = 'embryo.2d.edited.png', width=8.5, height=6, units = 'in',res=300, type = "cairo")
CustomHeatmapOnly3(object = downsampled.embryo.2d,
                   data.type = 'RNA', # sets the assay to pull data from
                   primary = 'Condition' , # sets the first metadata slot to use (primary column grouping)
                   secondary = 'SampleRearranged' , # sets the second metadata slot to use (secondary column grouping)
                   #tertiary = 'Sample' , # sets the third metadata slot to use (tertiary column grouping)
                   #quarternary = 'orig.ident' , # not used in this specific function
                   primary.cols = col.pal$Condition, # Needs to be a named list of colors
                   secondary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #tertiary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #quarternary.cols = NULL, # not used in this specific function
                   features = Figure_3_OrderGenes, # defined above, customize
                   labels = c('Condition','Sample'), # Needs to ordered IDENTICALLY to primary, secondary, tertiary, above, or will produce inaccurately labeled plot
                   selected.row.anotations=Figure_3_OrderGenes_Interest,
                   selected.label.size = 9,
                   use.scale.data = T,
                   range.frac = 0.2)
dev.off()

png(file = 'embryo.2d.edited.alt.png', width=16, height=12, units = 'in',res=300, type = "cairo")
CustomHeatmapOnly3(object = downsampled.embryo.2d,
                   data.type = 'RNA', # sets the assay to pull data from
                   primary = 'Condition' , # sets the first metadata slot to use (primary column grouping)
                   secondary = 'SampleRearranged' , # sets the second metadata slot to use (secondary column grouping)
                   #tertiary = 'Sample' , # sets the third metadata slot to use (tertiary column grouping)
                   #quarternary = 'orig.ident' , # not used in this specific function
                   primary.cols = col.pal$Condition, # Needs to be a named list of colors
                   secondary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #tertiary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #quarternary.cols = NULL, # not used in this specific function
                   features = Figure_3_OrderGenes, # defined above, customize
                   labels = c('Condition','Sample'), # Needs to ordered IDENTICALLY to primary, secondary, tertiary, above, or will produce inaccurately labeled plot
                   selected.row.anotations=Figure_3_OrderGenes,
                   selected.label.size = 9,
                   use.scale.data = T,
                   range.frac = 0.2)
dev.off()

downsampled.2dto3d <- subset(downsampled, subset = Condition == c("P0-2D", "P3-2D", "Engineered-24Hr"))

downsampled$Condition

downsampled.2dto3d <- RenameIdents(downsampled.2dto3d,
                                   "Engineered-24Hr" = "24 Hour")
unique(Idents(downsampled.2dto3d))
downsampled.2dto3d$ConditionRearranged <- Idents(downsampled.2dto3d)

downsampled.2dto3d$ConditionRearranged <- factor(downsampled.2dto3d$ConditionRearranged, levels = c("P0-2D", "P3-2D", "24 Hour"))


  VlnPlot(downsampled.2dto3d, features = "Cnfn")
  


png(file = 'TwoDto3D.Upreg.edit.v2.interest.png', width=8.5, height=6, units = 'in',res=300, type = "cairo")
CustomHeatmapOnly3(object = downsampled.2dto3d,
                   data.type = 'RNA', # sets the assay to pull data from
                   primary = 'ConditionRearranged' , # sets the first metadata slot to use (primary column grouping)
                   secondary = 'SampleRearranged' , # sets the second metadata slot to use (secondary column grouping)
                   #tertiary = 'Sample' , # sets the third metadata slot to use (tertiary column grouping)
                   #quarternary = 'orig.ident' , # not used in this specific function
                   primary.cols = c("green", "yellow", "purple"), # Needs to be a named list of colors
                   secondary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #tertiary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #quarternary.cols = NULL, # not used in this specific function
                   features = Figure_6_OrderGenes, # defined above, customize
                   labels = c('Condition','Sample'), # Needs to ordered IDENTICALLY to primary, secondary, tertiary, above, or will produce inaccurately labeled plot
                   selected.row.anotations=Figure_6_OrderGenes_Interest,
                   selected.label.size = 9,
                   use.scale.data = T,
                   range.frac = 0.2)
dev.off()

png(file = 'TwoDto3D.Upreg.edit.v2.interest.Alt.png', width=16, height=12, units = 'in',res=300, type = "cairo")
CustomHeatmapOnly3(object = downsampled.2dto3d,
                   data.type = 'RNA', # sets the assay to pull data from
                   primary = 'ConditionRearranged' , # sets the first metadata slot to use (primary column grouping)
                   secondary = 'SampleRearranged' , # sets the second metadata slot to use (secondary column grouping)
                   #tertiary = 'Sample' , # sets the third metadata slot to use (tertiary column grouping)
                   #quarternary = 'orig.ident' , # not used in this specific function
                   primary.cols = c("green", "yellow", "purple"), # Needs to be a named list of colors
                   secondary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #tertiary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #quarternary.cols = NULL, # not used in this specific function
                   features = Figure_6_OrderGenes, # defined above, customize
                   labels = c('Condition','Sample'), # Needs to ordered IDENTICALLY to primary, secondary, tertiary, above, or will produce inaccurately labeled plot
                   selected.row.anotations=Figure_6_OrderGenes,
                   selected.label.size = 10,
                   use.scale.data = T,
                   range.frac = 0.2)
dev.off()

downsampled.3dto5d <- subset(downsampled, subset = Condition == c("P3-2D", "Engineered-24Hr", "Engineered-FiveDay"))

downsampled$Condition

Idents(downsampled.3dto5d) <- downsampled.3dto5d$ConditionRearranged
downsampled.3dto5d <- RenameIdents(downsampled.3dto5d,
                                   "Engineered-24Hr" = "24 Hour",
                                   "Engineered-FiveDay" = "Five Day"
)
downsampled.3dto5d$ConditionRearranged <- Idents(downsampled.3dto5d)
downsampled.3dto5d$ConditionRearranged <- factor(downsampled.3dto5d$ConditionRearranged, levels = c("P3-2D", "24 Hour", "Five Day"))
png(file = 'up.fiveday.interest.png', width=8.5, height=6, units = 'in',res=300, type = "cairo")
CustomHeatmapOnly3(object = downsampled.3dto5d,
                   data.type = 'RNA', # sets the assay to pull data from
                   primary = 'ConditionRearranged' , # sets the first metadata slot to use (primary column grouping)
                   secondary = 'SampleRearranged' , # sets the second metadata slot to use (secondary column grouping)
                   #tertiary = 'Sample' , # sets the third metadata slot to use (tertiary column grouping)
                   #quarternary = 'orig.ident' , # not used in this specific function
                   primary.cols = c("yellow", "purple", "orange"), # Needs to be a named list of colors
                   secondary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #tertiary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #quarternary.cols = NULL, # not used in this specific function
                   features = Figure_7_OrderGenes, # defined above, customize
                   labels = c('Condition','Sample'), # Needs to ordered IDENTICALLY to primary, secondary, tertiary, above, or will produce inaccurately labeled plot
                   selected.row.anotations=Figure_7_OrderGenes_Interest,
                   selected.label.size = 8,
                   use.scale.data = T,
                   range.frac = 0.2)
dev.off()

png(file = 'up.fiveday.interest.alt.png', width=16, height=12, units = 'in',res=300, type = "cairo")
CustomHeatmapOnly3(object = downsampled.3dto5d,
                   data.type = 'RNA', # sets the assay to pull data from
                   primary = 'ConditionRearranged' , # sets the first metadata slot to use (primary column grouping)
                   secondary = 'SampleRearranged' , # sets the second metadata slot to use (secondary column grouping)
                   #tertiary = 'Sample' , # sets the third metadata slot to use (tertiary column grouping)
                   #quarternary = 'orig.ident' , # not used in this specific function
                   primary.cols = c("yellow", "purple", "orange"), # Needs to be a named list of colors
                   secondary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #tertiary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #quarternary.cols = NULL, # not used in this specific function
                   features = Figure_7_OrderGenes, # defined above, customize
                   labels = c('Condition','Sample'), # Needs to ordered IDENTICALLY to primary, secondary, tertiary, above, or will produce inaccurately labeled plot
                   selected.row.anotations=Figure_7_OrderGenes,
                   selected.label.size = 10,
                   use.scale.data = T,
                   range.frac = 0.2)
dev.off()
removal <- c("Sema4g", "Ppp1r14a", "Vill")
up.fiveday <- setdiff(up.fiveday, removal)
png(file = 'down.fiveday.png', width=16, height=12, units = 'in',res=300, type = "cairo")
CustomHeatmapOnly3(object = downsampled.3dto5d,
                   data.type = 'RNA', # sets the assay to pull data from
                   primary = 'ConditionRearranged' , # sets the first metadata slot to use (primary column grouping)
                   secondary = 'SampleRearranged' , # sets the second metadata slot to use (secondary column grouping)
                   #tertiary = 'Sample' , # sets the third metadata slot to use (tertiary column grouping)
                   #quarternary = 'orig.ident' , # not used in this specific function
                   primary.cols = col.pal$Condition, # Needs to be a named list of colors
                   secondary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #tertiary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #quarternary.cols = NULL, # not used in this specific function
                   features = down.fiveday, # defined above, customize
                   labels = c('Condition','Sample'), # Needs to ordered IDENTICALLY to primary, secondary, tertiary, above, or will produce inaccurately labeled plot
                   selected.row.anotations=down.fiveday,
                   selected.label.size = 10,
                   use.scale.data = T,
                   range.frac = 0.2)
dev.off()


png(file = 'Merged.Obj.png', width=16, height=12, units = 'in',res=300, type = "cairo")
DimPlot(merged.obj, group.by = "ConditionRearranged", cols = c("red", "blue", "green", "yellow", "purple","orange"), shuffle = T) + NoAxes() + ggtitle("Merged Native and Epithelial Dataset, Grouped By Condition")
dev.off()

inflammation.module_genes <- c("Cxcl2", "Cxcl3", "Cxcl6", "Cxcl1", "Zc3h12a", "Melk")
angiogenesis.module_genes <- c("Adtrp", "Wisp1", "Angptl4", "Map3k6", "Mpst", "Epas1", "Cavin2")


png(file = 'angiogenesis.module_genes.png', width=16, height=12, units = 'in',res=300, type = "cairo")
CustomHeatmapOnly3(object = downsampled,
                   data.type = 'RNA', # sets the assay to pull data from
                   primary = 'ConditionRearranged' , # sets the first metadata slot to use (primary column grouping)
                   secondary = 'SampleRearranged' , # sets the second metadata slot to use (secondary column grouping)
                   #tertiary = 'Sample' , # sets the third metadata slot to use (tertiary column grouping)
                   #quarternary = 'orig.ident' , # not used in this specific function
                   primary.cols = col.pal$Condition, # Needs to be a named list of colors
                   secondary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #tertiary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #quarternary.cols = NULL, # not used in this specific function
                   features = angiogenesis.module_genes, # defined above, customize
                   labels = c('Condition','Sample'), # Needs to ordered IDENTICALLY to primary, secondary, tertiary, above, or will produce inaccurately labeled plot
                   selected.row.anotations=angiogenesis.module_genes,
                   selected.label.size = 10,
                   use.scale.data = T,
                   range.frac = 0.2)
dev.off()

png(file = 'inflammation.module_genes.png', width=16, height=12, units = 'in',res=300, type = "cairo")
CustomHeatmapOnly3(object = downsampled,
                   data.type = 'RNA', # sets the assay to pull data from
                   primary = 'ConditionRearranged' , # sets the first metadata slot to use (primary column grouping)
                   secondary = 'SampleRearranged' , # sets the second metadata slot to use (secondary column grouping)
                   #tertiary = 'Sample' , # sets the third metadata slot to use (tertiary column grouping)
                   #quarternary = 'orig.ident' , # not used in this specific function
                   primary.cols = col.pal$Condition, # Needs to be a named list of colors
                   secondary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #tertiary.cols = col.pal$SampleRearranged, # Needs to be a named list of colors
                   #quarternary.cols = NULL, # not used in this specific function
                   features = inflammation.module_genes, # defined above, customize
                   labels = c('Condition','Sample'), # Needs to ordered IDENTICALLY to primary, secondary, tertiary, above, or will produce inaccurately labeled plot
                   selected.row.anotations=inflammation.module_genes,
                   selected.label.size = 10,
                   use.scale.data = T,
                   range.frac = 0.25)
dev.off()

VlnPlot(merged.obj, features = "Pparg")
VlnPlot(merged.obj, features = "Zc3h12a", group.by = "ConditionRearranged")



